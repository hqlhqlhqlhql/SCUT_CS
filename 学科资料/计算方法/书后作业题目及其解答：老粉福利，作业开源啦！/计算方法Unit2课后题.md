### 于博宇 计算机科学与技术一班 202330453151
![[Pasted image 20260416131924.png]]
![[Pasted image 20260416132007.png]]
![[Pasted image 20260416132038.png]]
### 要求完成1、3、6、7、8题，如下所示：
### 题目1
给定函数 ``y=sinx`` 的函数表如表2-21所示，请分别用Lagrange和Newton插值公式求出 ``y=sinx`` 在 ``x=1.75`` 处的函数近似值，并进行误差估计。
![[Pasted image 20260416134427.png]]
已知节点：
``x0=1.74,y0=0.9857``；
``x1=1.76,y1=0.9822``；
``x2=1.78,y2=0.9782``
目标点：``x=1.75``
#### （1）Lagrange二次插值
基函数：
``l0(x)=(x-1.76)(x-1.78)/[(1.74-1.76)(1.74-1.78)]=(x-1.76)(x-1.78)/0.0008``

``l1(x)=(x-1.74)(x-1.78)/[(1.76-1.74)(1.76-1.78)]=(x-1.74)(x-1.78)/(-0.0004)``

``l2(x)=(x-1.74)(x-1.76)/[(1.78-1.74)(1.78-1.76)]=(x-1.74)(x-1.76)/0.0008``
代入 ``x=1.75``：

``L2(1.75)=0.9857*(0.0003/0.0008)+0.9822*(-0.0003/(-0.0004))+0.9782*(0.0001/0.0008)``
``=0.9839875≈0.9840``
#### （2）Newton二次插值
构造差商表：

| ``xi`` | ``yi`` | 一阶差商   | 二阶差商   |
| ------ | ------ | ------ | ------ |
| 1.74   | 0.9857 |        |        |
| 1.76   | 0.9822 | -0.175 |        |
| 1.78   | 0.9782 | -0.200 | -0.625 |
Newton插值多项式：
``N2(x)=0.9857-0.175*(x-1.74)-0.625*(x-1.74)*(x-1.76)``
代入 ``x=1.75``：
``N2(1.75)=0.9840``
#### （3）误差估计
``f(x)=sinx,f'''(x)=-cosx``
``|R2(x)| = (|f'''(ξ)|/6 ) * [ |(x-1.74)(x-1.76)(x-1.78)| ]``
``|R2(1.75)|≤cos(1.74)/6 * 0.000006≈8.75×10^-7``

### 题目3
给定函数 ``y=f(x)`` 的函数表如表2-22所示，分别用Lagrange插值和Newton插值构造三次插值多项式，并写出插值余项。
![[Pasted image 20260416134908.png]]
已知节点：
``x0=1,y0=4``；
``x1=2,y1=10``；
``x2=4,y2=1``；
``x3=6,y3=0``
#### （1）Lagrange三次插值
``L3(x)=Σ（i=0到3）yi*li(x)``
整理得：
``L3(x)=-5/6 x^3 + 21/2 x^2 - 104/3 x + 24``
#### （2）Newton三次插值
构造差商表：

|``xi``|``yi``|一阶差商|二阶差商|三阶差商|
|---|---|---|---|---|
|1|4||||
|2|10|6|||
|4|1|-9/2|-21/4||
|6|0|-1/2|7/8|-5/6|
Newton插值多项式：
``N3(x)=4+6*(x-1) - 21/4*(x-1)*(x-2) - 5/6*(x-1)*(x-2)*(x-4)``
#### （3）插值余项
``R3(x)=(f''''(ξ)/24 )* [(x-1)*(x-2)*(x-4)*(x-6)]``

### 题目6
给定函数表如表2-23所示，求分段线性插值函数 ``p(x)``，并求 ``f(2.5)`` 的近似值。
![[Pasted image 20260416135134.png]]
#### 分段线性插值函数
1. ``x∈[0,1]``：``p(x)=2+x``
2. ``x∈[1,3]``：``p(x)=x+2``
3. ``x∈[3,5]``：``p(x)=5 - 3/2*(x-3)``
#### 计算 ``f(2.5)``
``2.5∈[1,3]``，代入得：
``p(2.5)=2.5+2=4.5``

### 题目7
给定函数 ``y=f(x)`` 的函数表如表2-24所示，请构造一个三次Hermite插值函数，并求出 ``f(1.5)`` 的近似值。
![[Pasted image 20260416135252.png]]
已知：
``x0=1,y0=2,y0'=1``；
``x1=2,y1=3,y1'=-1``
三次Hermite插值公式：
``H3(x)=y0*α0(x)+y1*α1(x)+y0'*β0(x)+y1'*β1(x)``
``α0(x)=(1+2*(x-1)/(2-1))*((x-2)/(1-2))^2``
``α1(x)=(1+2*(x-2)/(1-2))*((x-1)/(2-1))^2``
``β0(x)=(x-1)*((x-2)/(1-2))^2``
``β1(x)=(x-2)*((x-1)/(2-1))^2``
化简得：
``H3(x)=-2x^3 + 15/2 x^2 - 11/2 x + 4``
计算 ``x=1.5``：
``H3(1.5)=3.125``

### 题目8
求一个次数不高于4次的代数多项式 ``p(x)``，使它满足条件：
``p(0)=p'(0)=0``；``p(1)=p'(1)=1``；``p(2)=1``。

由 ``p(0)=0,p'(0)=0``，设：
``p(x)=x^2*(a x^2 + b x + c)``
代入条件：
1. ``p(1)=a + b + c=1``
2. ``p'(1)=4a + 3b + 2c=1``
3. ``p(2)=4*(4a + 2b + c)=1``
解得：

``a=1/4, b=-1, c=1``
故：``p(x)=1/4 x^4 - x^3 + x^2``


于博宇同学你好！针对你提交的计算机科学与技术一班数值分析作业（第1、3、6、7、8题），我已经对你的文本解答进行了大门诊式的逐一核对。

总体来看，你的**解题思路和公式选用全部正确**，但在**基础计算、差商表构建以及多项式化简**上翻车比较严重。5道题中，完全正确的只有1道，其余4道均存在不同程度的计算错误。

以下是详尽的正误剖析与讲解：

## 题目1：$\sin x$ 的二次插值与误差估计

### 🔴 检查结果：结果凑对，但过程存在符号与计算错误

1. **Lagrange插值部分：**
    
    在计算 $l_2(1.75)$ 时，分子为 $(1.75-1.74)(1.75-1.76) = 0.01 \times (-0.01) = -0.0001$。你文本中误写成了正的 `0.0001`。不过好在最终算出的近似值 $0.9840$ 是正确的。
    
2. **Newton插值部分：**
    
    差商计算及多项式代入完全正确，$N_2(1.75) \approx 0.9840$。
    
3. **误差估计部分（严重错误）：**
    
    - **节点乘积错误：** $\omega(1.75) = |(1.75-1.74)(1.75-1.76)(1.75-1.78)| = 0.01 \times 0.01 \times 0.03 = 0.000003$，你写成了 `0.000006`。
        
    - **最值点找错：** 在区间 $[1.74, 1.78]$ 上，$|f'''(\xi)| = |-\cos \xi|$ 的最大值出现在右端点 $1.78$ 处（因为在这个区间内余弦值的绝对值是递增的），最大值约为 $0.2077$，而不是你写的 $\cos(1.74)$。
        
    - **正确误差上限：** $\frac{0.2077}{6} \times 0.000003 \approx 1.04 \times 10^{-7}$。你最后写出的 $8.75 \times 10^{-7}$ 与你的公式和正确答案都对不上。
        

## 题目3：三次插值多项式与余项

### ❌ 检查结果：完全错误（由于二阶差商算错，导致满盘皆输）

- **错误核心：** 你的**差商表从二阶开始就崩塌了**。
    
    在计算二阶差商 $f[x_0, x_1, x_2]$ 时，公式为 $\frac{f[x_1, x_2] - f[x_0, x_1]}{x_2 - x_0}$。
    
    正确的分母应该是 $x_2 - x_0 = 4 - 1 = 3$，计算结果应为 $\frac{-4.5 - 6}{3} = -3.5 = -\frac{7}{2}$。你错算成了 $-\frac{21}{4}$（你分母错除以了 2）。
    
- **连锁反应：** 基础差商错了，导致你的三阶差商（正确应为 $0.9$ 或 $\frac{9}{10}$）、Lagrange整理式和Newton多项式全错。
    
- **正确答案参考：**
    
    - 正确的三次插值多项式为：
        
        $$P_3(x) = \frac{9}{10}x^3 - \frac{49}{5}x^2 + \frac{291}{10}x - \frac{81}{5}$$
        
    - 插值余项的**公式形式**是正确的：$R_3(x)=\frac{f^{(4)}(\xi)}{24}(x-1)(x-2)(x-4)(x-6)$。
        

## 题目6：分段线性插值

### 检查结果：完全正确

- **讲解：** 这一题完成得很好。区间划分 $2.5 \in [1, 3]$ 定位准确。在该区间内的线性表达式 $p(x) = x + 2$构造正确，代入算得 $p(2.5) = 4.5$，全对！
    

## 题目7：三次 Hermite 插值

### ❌ 检查结果：错误（基函数全对，但最后化简合并翻车）

- **原因分析：** 你的基函数 $\alpha_0(x), \alpha_1(x), \beta_0(x), \beta_1(x)$ 的**公式写得极其漂亮且完全正确**。然而，在最后一步将它们乘以系数合并化简为 $H_3(x) = 2\alpha_0 + 3\alpha_1 + \beta_0 - \beta_1$ 时，你的多项式展开出现了严重的代数计算错误。
    
- **反向检验：** 拿你的答案 $H_3(x)=-2x^3 + \frac{15}{2} x^2 - \frac{11}{2} x + 4$ 代入验证，当 $x=1$ 时算出来值是 $4$，不等于题目给的 $y_0 = 2$。
    
- **正确答案参考：**
    
    - 正确化简后的多项式应为：
        
        $$H_3(x) = -2x^3 + 8x^2 - 9x + 5$$
        
    - 将 $x=1.5$ 代入正确的多项式，得到的近似值应为：
        
        $$H_3(1.5) = 2.75$$
        
        （你算成了 3.125）。
        

## 题目8：不高于4次的多项式构造

### ❌ 检查结果：错误（设元和列方程极聪明，但解方程算错）

- **原因分析：** 你的设元思路非常高明！利用 $p(0)=p'(0)=0$，直接设 $p(x)=x^2(ax^2+bx+c)$，这瞬间干掉了两个未知数。列出的三元一次方程组也完全正确。
    
- **翻车点：** 依然是最后的计算。你解出来的 $a=\frac{1}{4}, b=-1, c=1$ 只要带回第一个方程 $a+b+c=1$ 就会发现 $\frac{1}{4} - 1 + 1 = \frac{1}{4} \neq 1$，根本对不上。
    
- **正确答案参考：**
    
    - 严谨求解该方程组后，得到的系数应为：$a = \frac{1}{4}, b = -\frac{3}{2}, c = \frac{9}{4}$。
        
    - 故正确的多项式为：
        
        $$p(x) = \frac{1}{4}x^4 - \frac{3}{2}x^3 + \frac{9}{4}x^2$$
        

### 💡 总结复习建议

于同学，作为计算机专业的学生，你的算法逻辑（如何设元、怎么套公式）是完全通顺的。目前的失分点全部集中在**算术细节（正负号、分母的选择、多项式乘法展开、三元一次方程组求解）**。

建议在接下来的期末复习或交作业前，利用好“代入原点检验法”。比如题目8和题目7，算完最终表达式后，花10秒钟把 $x=1$ 或 $x=0$ 代进去看看等不等于原条件，这样就能在交卷前自己揪出这几处计算错误！